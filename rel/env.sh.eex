#!/bin/sh

# Robine's MVP is a single-host HTTP application. It deliberately exposes no
# Erlang Distribution listener, node name, or cookie surface.
export RELEASE_DISTRIBUTION=none
unset RELEASE_NODE

