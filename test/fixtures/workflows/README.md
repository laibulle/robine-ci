# Workflow validation corpus

These fixtures are the compatibility contract shared by the server facade and CLI. `manifest.json` records whether each document is valid and the ordered stable diagnostic codes expected for invalid documents.
